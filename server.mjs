import {createServer} from 'node:http';
import {readFile} from 'node:fs/promises';
import handler from './api/index.mjs';
const files={'/':'index.html',...Object.fromEntries(['pwa.js','sw.js','manifest.webmanifest','icon-192.png','icon-512.png','icon-maskable.png','apple-touch-icon.png'].map(f=>['/'+f,f])),'/app.js':'app.js','/qrcode.js':'qrcode.js','/style.css':'style.css','/asl-logo.svg':'asl-logo.svg','/favicon.svg':'favicon.svg','/inter.woff2':'inter.woff2','/mono.woff2':'mono.woff2'};
createServer(async(req,res)=>{const path=new URL(req.url,'http://localhost').pathname;if(path==='/api')return handler(req,res);const file=files[path]||(/^\/audio\/[a-z0-9-]+\.mp3$/.test(path)?path.slice(1):path==='/audio.js'?'audio.js':null);if(!file){res.writeHead(404);res.end('Not found');return;}try{const data=await readFile(new URL(`./public/${file}`,import.meta.url));res.writeHead(200,{'Content-Type':file.endsWith('.webmanifest')?'application/manifest+json':file.endsWith('.png')?'image/png':file.endsWith('.mp3')?'audio/mpeg':file.endsWith('.html')?'text/html; charset=utf-8':file.endsWith('.css')?'text/css':file.endsWith('.svg')?'image/svg+xml':file.endsWith('.woff2')?'font/woff2':'text/javascript','Cache-Control':'no-store','X-Content-Type-Options':'nosniff'});res.end(data);}catch{res.writeHead(500);res.end('Could not load app');}}).listen(Number(process.env.PORT||4187),'0.0.0.0',()=>console.log('Mafia host listening'));
